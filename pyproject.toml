[tool.poetry]
name = "rtml-core"
version = "0.1.0"
description = ""
authors = ["DeadMeme <deadunderscorememe@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
